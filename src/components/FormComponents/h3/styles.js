import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    header3: {
        fontSize: 22,
        letterSpacing: 0.35,
        lineHeight: 28,
        margin: 10,
    }
});

export default styles;
